// const DatabaseError = require('../../utils/errors/databaseError');
// const ValidationError = require('../../utils/errors/validationError');

// const { queryPromise } = require('../../utils/queryPromise');
// const {
//   formatDate, formatBoolean, formatNumber, formatArray, atLeastOneDefined, areAllDefined,
// } = require('../../utils/validateFormat');

/**
 *  Queries the database to update a family. If the query is successful, then returns
 *  If a problem is encountered, creates and throws custom error
 */

// eslint-disable-next-line no-unused-vars
const updateFamilyQuery = async (req) => {

};

module.exports = { updateFamilyQuery };
